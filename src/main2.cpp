#include <bits/stdc++.h>
#include "big_int.h"
using namespace std;
int main() {
    BigInt x, y;
    cin >> x >> y;
    cout << x / y;
        // BigInt x;
        // int y;
        // cin >> x >> y;
        // cout << (x / y);
}
    